require 'spec_helper'

describe EasyGraph do
  it 'has a version number' do
    expect(EasyGraph::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
